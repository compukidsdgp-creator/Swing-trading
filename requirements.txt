# Minimum versions, not exact pins.
#
# An earlier attempt at exact pinning broke the deployment: the pins were
# written from memory rather than measured, and named versions well behind what
# the code actually runs on (yfinance 0.2.51 against the 1.5.2 it was built and
# tested with — a major version apart).
#
# Lower bounds give most of the protection without that risk. They stop a
# resolver installing something ancient, while letting Streamlit Cloud use the
# versions it actually has available.
#
# If you do want exact pins later, generate them from a working deployment
# rather than by hand:
#     pip freeze > requirements.lock.txt
# then verify with `python test_invariants.py 50` before committing.

streamlit>=1.40
yfinance>=1.0
pandas>=2.2
numpy>=1.26
plotly>=5.20
requests>=2.31
openpyxl>=3.1

# Optional — Paytm Money broker integration (read-only cost measurement).
# NOT on PyPI; install from GitHub:
#     pip install git+https://github.com/paytmmoney/pyPMClient.git

# Optional — PDF report generation. Needs system libraries (pango, cairo) that
# Streamlit Cloud does not provide, so it is installed only in CI workflows.
# Locally:  pip install weasyprint
