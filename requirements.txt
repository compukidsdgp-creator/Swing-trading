# Pinned deliberately.
#
# yfinance breaks regularly when Yahoo changes its API, and pandas 3.0 altered
# dtype-assignment behaviour in a way that already caused a bug here. Floating
# versions mean an unattended workflow can fail one morning for reasons entirely
# unrelated to your code.
#
# To upgrade: bump one line, run `python test_invariants.py 50`, and only then
# commit. The CI workflow will confirm.

streamlit==1.41.1
yfinance==0.2.51
pandas==2.2.3
numpy==1.26.4
plotly==5.24.1
requests==2.32.3
openpyxl==3.1.5

# Optional — Paytm Money broker integration (read-only cost measurement).
# NOT on PyPI; install from GitHub:
#     pip install git+https://github.com/paytmmoney/pyPMClient.git

# Optional — PDF report generation. Needs system libraries (pango, cairo) that
# Streamlit Cloud does not provide, so it is installed only in CI workflows.
# Locally:  pip install weasyprint
